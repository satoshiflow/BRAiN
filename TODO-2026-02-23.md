# TODO Liste - 2026-02-23

## Heutige Ziele

### 1. Skill System Completion (läuft im Hintergrund)
- [ ] Auto-seed Built-in Skills beim Server-Start
- [ ] `is_builtin` Flag in DB
- [ ] API: Builtins aktivieren/deaktivieren (statt löschen)
- [ ] UI: Toggle für Builtins statt Delete-Button

### 2. Core-nahe Module mit EventStream (5 Stück)

**Reihenfolge (Core-nah zuerst):**

1. **Agent Management System**
   - Agent Registry / Discovery
   - Agent Lifecycle (register, heartbeat, unregister)
   - EventStream: agent.registered, agent.heartbeat, agent.offline

2. **Task/Job Queue System** 
   - Erweitertes Mission Queue
   - Job Priorisierung & Scheduling
   - EventStream: task.created, task.started, task.completed, task.failed

3. **Health & Monitoring**
   - System Health Checks
   - Service Status Monitoring
   - EventStream: health.check, health.degraded, health.recovered

4. **Configuration Management**
   - Dynamic Config Store
   - Config Changes & Rollouts
   - EventStream: config.changed, config.deployed

5. **Audit & Logging**
   - Centralized Audit Trail
   - Operation Logging
   - EventStream: audit.operation, audit.security

### 3. EventStream Infrastructure
- [ ] SSE Endpoint standardisieren
- [ ] EventBus für interne Kommunikation
- [ ] Client Reconnect-Logik
- [ ] Event Persistence (letzte 100 Events für neue Clients)

## Abhängigkeiten
```
Skill System -> Agent Management -> Task Queue -> Health -> Config -> Audit
```

## Notizen
- Core-nahe = andere Module bauen darauf auf
- EventStream = Real-time Updates + Audit Trail
- Jedes Modul: API + Events + UI Integration
